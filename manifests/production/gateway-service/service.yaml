apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: production
  labels:
    app: gateway-service
    environment: production
spec:
  type: ClusterIP
  selector:
    app: gateway-service
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
