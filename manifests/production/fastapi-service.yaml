apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: production
  labels:
    app: fastapi-app
    environment: production
spec:
  type: ClusterIP
  selector:
    app: fastapi-app
    environment: production
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
