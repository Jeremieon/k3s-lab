apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-service-config
  namespace: dev
  labels:
    app: gateway-service
    environment: dev
data:
  APP_VERSION: "1.0.0-dev"
  ENVIRONMENT: "dev"
  LOG_LEVEL: "debug"
  ITEMS_SERVICE_URL: "http://items-service.dev.svc.cluster.local"
  TIMEOUT_SECONDS: "10"
