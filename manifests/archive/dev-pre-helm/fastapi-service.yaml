apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: dev
  labels:
    app: fastapi-app
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: fastapi-app
    environment: dev
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
