apiVersion: v1
kind: Service
metadata:
  name: items-service
  namespace: production
  labels:
    app: items-service
    environment: production
spec:
  type: ClusterIP
  selector:
    app: items-service
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
